pub fn dp_rec_mc(amount: u32) -> u32 {
    // if amount <= 0 {
    //     return 0;
    // }
    // let coins = vec![1, 2, 5, 10, 20, 50, 100, 200];
    // let mut count = 0;
    // for coin in coins.iter().rev() {
    //     if amount >= *coin {
    //         count += dp_rec_mc(amount - *coin);
    //     }
    // }
    // count
    0
}
