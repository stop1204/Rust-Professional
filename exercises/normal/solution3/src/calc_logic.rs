pub fn new_birthday_probability(n: u32) -> f64 {
    //一年內某天是 1 個人的生日概率是 365/365
    // 2 個人的不同生日概率是 365/365 * 364/365
    // 1-不同的生日概率=相同的生日概率 (1-365/365 * 364/365=1-364/365=1/365)

    let mut prob = 1.0;
    for i in 0..n {
        prob *= (365.0 - i as f64) / 365.0;
    }
    1.0 - prob
}
